#include "attaque.h"

ListeM remplir_liste_M(unsigned int message){
    ListeM LM;
    for(int i=0; i<5; i++){
       // LM.chiffre_obtenu[i]=chiffrement(LM.message[a],K); //On stocke le résultat du chiffrement du message clair avec la clee ki
    }
    return LM;
}